import React, { Component } from 'react';

class List extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <ul>
                {/*{this.props.searchResults.map((text, i) => (*/}
                {/*    console.log()*/}
                {/*    // <UserCard email={user.email}*/}
                {/*    //           message={user.message}*/}
                {/*    //           key={i}/>*/}
                {/*))}*/}
            </ul>
        )
    }
}

export default List;